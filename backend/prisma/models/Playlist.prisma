model Playlist {
  id         String   @id @default(cuid())
  
  name       String
  ownerId    String
  cover      String?
  description String?
  private    Boolean  @default(true)

  created_at  DateTime @default(now())

  music      PlaylistTrack[]
  user       User[]

  @@unique([name, ownerId])
}